Archived scripts in this folder are kept only for reference.

They represent one-off migrations, historical image workflows, or superseded import entrypoints that were removed from
the active `package.json` command surface during the perfume data cleanup.

Use the canonical commands at the repository root instead:

- `npm run perfumes:enrich`
- `npm run perfumes:verify`
- `npm run perfumes:import`
- `npm run prices:sync`
